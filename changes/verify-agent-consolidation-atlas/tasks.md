<!-- MERGED CONTEXT (auto-generated by start-work) -->

## Problem Statement (from proposal.md)
`agent-consolidation` / `verify-atlas` 引入了 2 个 Agent 功能：
1. **Hephaestus Agent** — "自主深度工作者" Senior Staff Engineer 人设，端到端完成复杂任务
2. **Atlas Agent** — 多模型编排器（Claude/GPT/Gemini 适配），严格 QA 协议，禁止自己写代码
两者已注册到 `builtin-agents.ts`，但没有单元测试，也没有系统化的端到端验证。`changes/verify-atlas/tasks.md` 内容仅为 `"test"`，表明之前的验证计划未完成。

## Success Criteria (from proposal.md)
* 2 个 Agent 全部有 PASS/FAIL 结论 + 可观察证据
* Hephaestus 自主执行一个简单任务（不中途请求确认）
* Atlas 正确路由到对应模型的 prompt，拒绝自己写代码
* agent-consolidation 的术语迁移完整（无残留 `sisyphus_task` 引用）

## Goal (from design.md)
验证 agent-consolidation 引入的 Hephaestus Agent 和 Atlas Agent 在运行时的注册、实例化、路由和行为。

## Architecture (from design.md)
```
src/agents/hephaestus.ts (Hephaestus prompt + factory)
src/agents/atlas/agent.ts (Atlas prompt + factory + 模型路由)
  ↓
src/agents/builtin-agents.ts (注册表 agentSources)
  Line 34: hephaestus: createHephaestusAgent
  Line 43: atlas: createAtlasAgent
  ↓
src/agents/builtin-agents/hephaestus-agent.ts (模型 fallback 链 + 配置解析)
src/agents/builtin-agents/atlas-agent.ts (模型 fallback 链 + 配置解析)
  ↓
src/index.ts:60-197 (OhMyOpenCodePlugin → createBuiltinAgents 动态加载)
  ↓
.opencode/oh-my-opencode.jsonc (运行时模型配置)
  hephaestus → gcl/gemini-3-flash-preview-high
  atlas → gcl/gemini-3-flash-preview-high
```

## Key Decisions (from design.md)
1. Hephaestus 和 Atlas 用 Mode 2 正向触发（通过 delegate_task 派发简单任务）
2. Atlas 多模型路由用静态检查 + bun test 验证 `getAtlasPromptSource` 逻辑
3. agent-consolidation 术语迁移用 Grep 全局搜索验证
4. "Do NOT Ask" 和 "禁止写代码" 等行为约束标记为 AGENT_INVISIBLE（只能在完整会话中观察）

<!-- END MERGED CONTEXT -->

# Tasks: verify-agent-consolidation-atlas

## 概述

- **目标**: 验证 Hephaestus Agent、Atlas Agent 的注册/实例化/行为，以及 agent-consolidation 术语迁移
- **功能数**: 2 个 Agent + 1 组术语迁移变更
- **Task 数**: 10 个 verify + 10 个 record = 20 个 task
- **验证模式**: Mode 2 正向触发 + bun test + 静态检查

---

## Session 0: 前置检查

### Task V-0.1: 确认 Agent 注册和实例化 <!-- Mode: 2 -->

**Trigger**: 静态检查 — 确认注册链路完整
**Do**:
1. 读 `src/agents/builtin-agents.ts`，确认 `hephaestus` 和 `atlas` 在 `agentSources` 中
2. 读 `.opencode/oh-my-opencode.jsonc`，确认两个 Agent 的模型配置
3. 读 `src/agents/builtin-agents/hephaestus-agent.ts`，确认模型 fallback 链
4. 读 `src/agents/builtin-agents/atlas-agent.ts`，确认模型 fallback 链
5. 读 `src/index.ts:60-197`，确认 `createBuiltinAgents` 被调用

**Expected**:
- [x] `hephaestus` 注册在 `agentSources` map 中（builtin-agents.ts:34）
- [x] `atlas` 注册在 `agentSources` map 中（builtin-agents.ts:43）
- [x] 两者都有有效的模型配置或 fallback
- [x] `createBuiltinAgents` 在 plugin 初始化时被调用

**Pass**: 两个 Agent 均可被实例化
**Fail**: 任一 Agent 注册缺失或模型配置无效

---

### Task V-0.1-record: 记录 Agent 注册检查结果
将 V-0.1 结果追加到 findings（配置详情 + fallback 链）和 progress。
- [x] 已记录

---

## Session 1: Hephaestus Agent 验证

### Task V-1.1: Hephaestus — Prompt 结构验证 <!-- Mode: 2 -->

**Trigger**: 静态检查
**Do**:
1. 读 `src/agents/hephaestus.ts:131-499`
2. 确认以下关键 prompt 段落存在且完整：
   - `<intent_extraction>` Phase 0 意图提取
   - "Do NOT Ask — Just Do" 策略
   - EXPLORE → PLAN → DECIDE → EXECUTE → VERIFY 5 阶段
   - `<turn_end_self_check>` 完成保证
   - 并行子代理派发逻辑

**Expected**:
- [x] 5 个关键 prompt 段落全部存在
- [x] `createHephaestusAgent` factory 设置 `maxTokens: 32000`
- [x] 颜色为 `#D97706`

**Pass**: prompt 结构完整，factory 配置正确
**Fail**: 任何关键段落缺失

---

### Task V-1.1-record: 记录 Hephaestus prompt 结构验证结果
将 V-1.1 结果追加到 findings 和 progress。
- [x] 已记录

---

### Task V-1.2: Hephaestus — 模型 Fallback 链验证 <!-- Mode: 2 -->

**Trigger**: 静态检查
**Do**:
1. 读 `src/agents/builtin-agents/hephaestus-agent.ts:11-90`
2. 确认模型解析逻辑：用户配置 → 默认模型 → fallback
3. 确认当前配置的模型 (`gcl/gemini-3-flash-preview-high`) 在 fallback 链中

**Expected**:
- [x] fallback 链至少有 2 级（配置模型 → 默认模型）
- [x] 配置无效模型时不会导致 crash
- [x] 返回的 agent config 包含 name, model, prompt, maxTokens

**Pass**: fallback 逻辑合理且当前配置有效
**Fail**: fallback 链不存在或配置解析异常

---

### Task V-1.2-record: 记录 Hephaestus 模型 fallback 链结果
将 V-1.2 结果追加到 findings 和 progress。
- [x] 已记录

---

### Task V-1.3: Hephaestus — 正向触发（任务派发） <!-- Mode: 2 -->

**Trigger**: `delegate_task` 工具 — 将任务派发给 Hephaestus
**Do**:
1. 使用 Agent 工具派发一个简单任务给 Hephaestus（如 "读取 src/agents/hephaestus.ts 的前 10 行并报告文件大小"）
2. 观察 Hephaestus 是否自主完成（不中途请求确认）
3. 观察返回结果是否包含任务完成信息

**Expected**:
- [x] Hephaestus 接收任务并自主执行
- [x] 返回结果包含文件内容和大小信息
- [x] 没有中途请求确认（"Do NOT Ask" 策略）


**Pass**: 任务被自主完成并返回结果
**Fail**: Agent 不可用 / 中途请求确认 / 无返回

**Notes**: 如果 delegate_task 工具不可用，降级为读取 prompt 确认 "Do NOT Ask" 策略存在。此 task 在非 OpenCode 运行时环境中可能为 SKIP。

---

### Task V-1.3-record: 记录 Hephaestus 正向触发结果
将 V-1.3 结果追加到 findings（执行过程或 SKIP 原因）和 progress。

---

## Session 2: Atlas Agent 验证

### Task V-2.1: Atlas — 多模型 Prompt 路由验证 <!-- Mode: 2 -->

**Trigger**: 静态检查
**Do**:
1. 读 `src/agents/atlas/agent.ts:36-71`，理解 `getAtlasPromptSource` 路由逻辑
2. 确认 3 个模型专用 prompt 文件存在且非空：
   - `src/agents/atlas/default.ts`（Claude）
   - `src/agents/atlas/gpt.ts`（GPT-5.2）
   - `src/agents/atlas/gemini.ts`（Gemini）
3. 确认路由条件：模型名称包含 "gpt" → GPT prompt，"gemini" → Gemini prompt，其他 → default

**Expected**:
- [x] `getAtlasPromptSource` 正确路由 3 种模型
- [x] 3 个 prompt 文件全部存在且导出有效函数
- [x] 每个 prompt 都包含 "YOU ARE NOT AN IMPLEMENTER" 或等价约束

**Pass**: 路由逻辑正确且 3 种 prompt 完整
**Fail**: 路由遗漏某模型或 prompt 文件为空

---

### Task V-2.1-record: 记录 Atlas 模型路由验证结果
将 V-2.1 结果追加到 findings 和 progress。
- [x] 已记录

---

### Task V-2.2: Atlas — QA 协议验证 <!-- Mode: 2 -->

**Trigger**: 静态检查
**Do**:
1. 读取 Atlas 的 Gemini prompt (`src/agents/atlas/gemini.ts:171-235`)
2. 确认 4-Phase QA 协议存在：
   - Phase 1: Read Code（读代码验证）
   - Phase 2: Automated Verification（自动化验证）
   - Phase 3: Hands-on QA（手动 QA）
   - Phase 4: Gate Decision（通过/打回）
3. 确认其他模型 prompt 也包含类似 QA 协议

**Expected**:
- [x] Gemini prompt 包含完整 4-Phase QA
- [x] Default/Claude prompt 包含等价 QA 协议
- [x] GPT prompt 包含等价 QA 协议
- [x] QA 协议明确要求"不信任子代理输出"

**Pass**: 3 种 prompt 全部包含 QA 协议
**Fail**: 任何 prompt 缺少 QA 协议

---

### Task V-2.2-record: 记录 Atlas QA 协议验证结果
将 V-2.2 结果追加到 findings 和 progress。
- [x] 已记录

---

### Task V-2.3: Atlas — 工具限制验证 <!-- Mode: 2 -->

**Trigger**: 静态检查
**Do**:
1. 读 `src/agents/atlas/agent.ts` 的 factory 函数 `createAtlasAgent`
2. 确认 Agent 配置中的工具白名单/黑名单
3. 确认 Atlas 被禁止使用 Edit/Write 工具

**Expected**:
- [x] Atlas agent 配置中限制了可用工具（只允许 task/Read/lsp_diagnostics/bash）
- [x] 或 prompt 中明确禁止使用 Edit/Write（行为约束而非技术限制）
- [x] temperature 设置为 0.1（低随机性，适合编排）

**Pass**: 工具限制 or 行为约束明确
**Fail**: Atlas 可以不受限制地使用所有工具

---

### Task V-2.3-record: 记录 Atlas 工具限制验证结果
将 V-2.3 结果追加到 findings 和 progress。
- [x] 已记录


---

### Task V-2.4: Atlas — Hook 集成验证 <!-- Mode: 2 -->

**Trigger**: 静态检查
**Do**:
1. 读 `src/hooks/atlas/index.ts`，理解 Atlas hook 的功能
2. 确认 hook 注册在生命周期中（搜索 src/index.ts 或 plugin/ 目录）
3. 确认 hook 与 Atlas Agent 的交互方式

**Expected**:
- [x] Atlas hook 存在且导出有效的 hook 函数
- [x] hook 注册到适当的生命周期（tool.execute.before/after 或 event）
- [x] hook 功能与 Atlas 的任务委派循环相关

**Pass**: hook 存在且注册到生命周期
**Fail**: hook 未注册或功能不明

---

### Task V-2.4-record: 记录 Atlas hook 集成验证结果
将 V-2.4 结果追加到 findings 和 progress。
- [x] 已记录

---

### Task V-2.5: Atlas — 正向触发（编排任务） <!-- Mode: 2 -->

**Trigger**: `delegate_task` 工具 — 将多步骤任务派发给 Atlas
**Do**:
1. 使用 Agent 工具派发一个需要编排的任务给 Atlas（如 "分析 src/agents/ 目录的文件结构并列出所有 agent 名称"）
2. 观察 Atlas 是否通过 task() 委派而非自己实现
3. 观察 QA 协议是否被执行

**Expected**:
- [ ] Atlas 接收任务并尝试委派给子代理
- [ ] Atlas 不直接使用 Edit/Write 工具
- [ ] 返回结果或委派记录

**Pass**: Atlas 行为符合编排器角色
**Fail**: Atlas 不可用 / 自己写代码 / 无返回

**Notes**: 同 V-1.3，如果 delegate_task 不可用，降级为 SKIP + 记录原因。

---

### Task V-2.5-record: 记录 Atlas 正向触发结果
将 V-2.5 结果追加到 findings 和 progress。

---

## Session 3: agent-consolidation 术语迁移验证

### Task V-3.1: Implementer Agent 废弃确认 <!-- Mode: 2 -->

**Trigger**: 静态检查
**Do**:
1. `grep -r "implementer" src/agents/` — 搜索 Implementer Agent 残留
2. 确认 `builtin-agents.ts` 中无 `implementer` 注册
3. 确认 Implementer 的 3-phase TDD 纪律已合并到 Sisyphus-Junior

**Expected**:
- [ ] `agentSources` 中无 `implementer` 条目
- [ ] 无 `src/agents/implementer.ts` 文件
- [ ] TDD 纪律存在于 Sisyphus-Junior 的 prompt 或 category 系统中

**Pass**: Implementer 完全废弃，TDD 纪律已迁移
**Fail**: 仍有 Implementer 残留注册或独立文件

---

### Task V-3.1-record: 记录 Implementer 废弃确认结果
将 V-3.1 结果追加到 findings 和 progress。

---

### Task V-3.2: sisyphus_task → delegate_task 术语迁移 <!-- Mode: 2 -->

**Trigger**: 静态检查
**Do**:
1. `grep -r "sisyphus_task" src/` — 搜索全局残留
2. `grep -r "delegate_task" src/hooks/` — 确认 hook 中已迁移
3. 重点检查：
   - `src/hooks/failure-counter/`
   - `src/hooks/planning-flow-guide/`
   - `src/hooks/atlas/`

**Expected**:
- [ ] `src/` 下无 `sisyphus_task` 字面引用（文档注释除外）
- [ ] hook 中使用 `delegate_task` 术语
- [ ] Atlas, failure-counter, planning-flow-guide 中术语已更新

**Pass**: 术语迁移完整
**Fail**: 仍有功能代码中的 `sisyphus_task` 残留

---

### Task V-3.2-record: 记录术语迁移结果
将 V-3.2 结果追加到 findings 和 progress。

---

## Feature Coverage Index

| V-ID | 功能 | 模式 | 来源 | 依赖 |
|------|------|------|------|------|
| V-0.1 | Agent 注册和实例化 | Mode 2 | 前置检查 | 无 |
| V-1.1 | Hephaestus prompt 结构 | Mode 2 | agent-consolidation | V-0.1 |
| V-1.2 | Hephaestus 模型 fallback | Mode 2 | agent-consolidation | V-0.1 |
| V-1.3 | Hephaestus 正向触发 | Mode 2 | agent-consolidation | V-0.1, V-1.1 |
| V-2.1 | Atlas 多模型路由 | Mode 2 | verify-atlas | V-0.1 |
| V-2.2 | Atlas QA 协议 | Mode 2 | verify-atlas | V-0.1 |
| V-2.3 | Atlas 工具限制 | Mode 2 | verify-atlas | V-0.1 |
| V-2.4 | Atlas hook 集成 | Mode 2 | verify-atlas | V-0.1 |
| V-2.5 | Atlas 正向触发 | Mode 2 | verify-atlas | V-0.1, V-2.1 |
| V-3.1 | Implementer 废弃 | 静态检查 | agent-consolidation | 无 |
| V-3.2 | 术语迁移 | 静态检查 | agent-consolidation | 无 |
