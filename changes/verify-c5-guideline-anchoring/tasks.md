<!-- MERGED CONTEXT (auto-generated by start-work) -->

## Problem Statement (from proposal.md)
`c5-guideline-anchoring` 引入了 2 个功能：

1. **behavior-anchor hook** — 使用 SlopDetector 监控 AI 输出质量，实时注入纠正指引
2. **slop-detector 共享模块** — 检测 AI 输出中的低质量模式（过度注释、冗长、重复）

这两个功能已有单元测试，但从未进行过真实行为触发验证。需要确认在运行时环境中 hook 是否正确触发并注入 guidelines。

## Success Criteria (from proposal.md)
* 2 个功能全部有 PASS/FAIL 结论 + 可观察证据
* slop-detector 3 种检测模式（过度注释、冗长解释、重复代码）分别验证
* behavior-anchor 在 tool.execute.after 中正确追加 guidelines 到 output.output
* 确认 `isHookEnabledLoose("behavior-anchor")` 在当前环境下返回 true

## Goal (from design.md)
验证 c5-guideline-anchoring 引入的 behavior-anchor hook 和 slop-detector 模块在运行时的真实行为。

## Architecture (from design.md)
```
src/shared/slop-detector.ts (纯函数 — 检测 3 种 slop 模式)
  ↓ 被调用
src/hooks/behavior-anchor/index.ts (hook — tool.execute.after)
  ↓ 注册
src/index.ts:208-210 (实例化 — isHookEnabledLoose)
  ↓ 接入
src/index.ts:468-471 (tool.execute.after 生命周期)
```

## Key Decisions (from design.md)
1. slop-detector 用 bun test 验证 3 种规则 + Mode 2 验证其调用方（铁律6）
2. behavior-anchor 用 Mode 2 正向触发 — 构造含 slop 的工具输出场景
3. 需要额外验证 refreshInterval 机制（每 10 轮自动注入）

<!-- END MERGED CONTEXT -->

# Tasks: verify-c5-guideline-anchoring

## 概述

- **目标**: 验证 c5-guideline-anchoring 引入的 behavior-anchor hook 和 slop-detector 模块
- **功能数**: 2（1 个 hook + 1 个共享模块）
- **Task 数**: 7 个 verify + 7 个 record = 14 个 task
- **验证模式**: bun test + Mode 2 正向触发

---

## Session 0: 前置检查

### Task V-0.1: 确认 behavior-anchor hook 启用状态 [x]
### Task V-0.1-record: 记录启用状态检查结果 [x]
将 V-0.1 结果追加到 findings（配置详情）和 progress（状态 + 时间戳）。

---

## Session 1: slop-detector 单元测试验证

### Task V-1.1: slop-detector — 单元测试 [x]
### Task V-1.1-record: 记录 slop-detector 单元测试结果 [x]
将 V-1.1 结果追加到 findings（pass/fail 数量 + 覆盖的规则）和 progress。

---

## Session 2: behavior-anchor 单元测试 + 正向触发

### Task V-2.1: behavior-anchor — 单元测试 [x]
### Task V-2.1-record: 记录 behavior-anchor 单元测试结果 [x]
将 V-2.1 结果追加到 findings 和 progress。

---

### Task V-2.2: behavior-anchor — 正向触发（过度注释检测） [x]
### Task V-2.2-record: 记录过度注释检测正向触发结果 [x]
将 V-2.2 结果追加到 findings（注入的 guidelines 内容或失败原因）和 progress。

---

### Task V-2.3: behavior-anchor — 正向触发（冗长解释检测） [x]
### Task V-2.3-record: 记录冗长解释检测正向触发结果 [x]
将 V-2.3 结果追加到 findings 和 progress。

---

### Task V-2.4: behavior-anchor — 正向触发（重复代码检测） [x]
### Task V-2.4-record: 记录重复代码检测正向触发结果 [x]
将 V-2.4 结果追加到 findings 和 progress。

---

### Task V-2.5: behavior-anchor — refreshInterval 机制验证 [x]
### Task V-2.5-record: 记录 refreshInterval 机制验证结果 [x]
将 V-2.5 结果追加到 findings 和 progress。

---

## Feature Coverage Index

| V-ID | 功能 | 模式 | 来源 | 依赖 |
|------|------|------|------|------|
| V-0.1 | behavior-anchor 启用状态 | Mode 2 | 前置检查 | 无 |
| V-1.1 | slop-detector 单元测试 | bun test | c5-guideline-anchoring | V-0.1 |
| V-2.1 | behavior-anchor 单元测试 | bun test | c5-guideline-anchoring | V-0.1 |
| V-2.2 | 过度注释检测 正向触发 | Mode 2 | c5-guideline-anchoring | V-0.1, V-1.1 |
| V-2.3 | 冗长解释检测 正向触发 | Mode 2 | c5-guideline-anchoring | V-0.1, V-1.1 |
| V-2.4 | 重复代码检测 正向触发 | Mode 2 | c5-guideline-anchoring | V-0.1, V-1.1 |
| V-2.5 | refreshInterval 定期注入 | Mode 2 | c5-guideline-anchoring | V-0.1 |
