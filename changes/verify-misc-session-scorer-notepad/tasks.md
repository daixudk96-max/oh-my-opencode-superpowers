<!-- MERGED CONTEXT (auto-generated by start-work) -->

## Problem Statement (from proposal.md)
"其他零散新增"（section 19）中有 4 个功能，其中 boulder-state 和 run-continuation-state 已在 `verify-fix-continuation-start-work` 中覆盖。剩余 2 个功能从未被系统化验证：

1. **session-scorer** (`src/features/session-scorer/`) — 会话质量评分 (Grade A-F)，基于测试覆盖(40%)、代码质量(30%)、任务完成度(30%)。来自 50-enhancements，在 `session.stop` 事件时输出评分。
2. **sisyphus-junior-notepad** hook (`src/hooks/sisyphus-junior-notepad/`) — Sisyphus Junior agent 的条件性笔记本，拦截 Atlas 编排器的 `task` 调用，注入 findings.md 写入指令，防止子代理陷入重复失败循环。来自 50-enhancements。

两者都有单元测试，但缺乏运行时行为验证。

## Success Criteria (from proposal.md)
* session-scorer 在 session.stop 时正确输出 `会话质量: X (N/100)` 格式的评分
* sisyphus-junior-notepad 在 Atlas 编排器调用 task 时正确注入 NOTEPAD_DIRECTIVE
* 两个功能的单元测试全部通过

## Goal (from design.md)
验证 session-scorer 和 sisyphus-junior-notepad 在运行时的注册、实例化和行为。

## Architecture (from design.md)
```
src/features/session-scorer/index.ts (评分引擎)
  ├─ 评分标准: 测试覆盖(40%) + 代码质量(30%) + 任务完成度(30%)
  ├─ 等级: A(≥90) B(≥80) C(≥70) D(≥60) F(<60) N/A(无数据)
  └─ 触发: session.stop 事件
      ↓ 注册
  src/index.ts → sessionScorer 实例
      ↓ 接入
  event handler → session.stop → 输出 "会话质量: X (N/100)"

src/hooks/sisyphus-junior-notepad/hook.ts (条件性笔记本注入)
  ├─ 触发条件: tool === "task" && isCallerOrchestrator && !已注入
  ├─ 注入内容: NOTEPAD_DIRECTIVE（强制用 Edit append findings.md）
  └─ 防重复: 检查 SYSTEM_DIRECTIVE_PREFIX 是否已存在
      ↓ 注册
  src/config/schema/hooks.ts → HookNameSchema "sisyphus-junior-notepad"
  src/plugin/hooks/create-session-hooks.ts → safeHook 实例化
      ↓ 接入
  tool.execute.before → 拦截 task 调用 → 修改 args
```

## Key Decisions (from design.md)
* session-scorer: 无修改文件 + 无任务 → Grade N/A
* session-scorer: lintErrors 极多导致 codeQuality < 0 → 应 clamp 到 0
* sisyphus-junior-notepad: 非 Atlas 调用 task → 不注入（正确行为）
* sisyphus-junior-notepad: 已注入后再次调用 task → 不重复注入（防重复机制）

<!-- END MERGED CONTEXT -->

# Tasks: verify-misc-session-scorer-notepad

## 概述

- **目标**: 验证 session-scorer 和 sisyphus-junior-notepad 的功能
- **功能数**: 2（1 个 feature + 1 个 hook）
- **Task 数**: 8 个 verify + 8 个 record = 16 个 task
- **验证模式**: bun test + Mode 2 正向触发 + 静态检查

---

## Session 0: 前置检查

### Task V-0.1: 确认两个功能的注册状态 <!-- Mode: 2 -->

**Trigger**: 静态检查
**Do**:
1. 读 `src/index.ts`，确认 session-scorer 被实例化和注册到 event handler
2. 读 `src/config/schema/hooks.ts`，确认 `"sisyphus-junior-notepad"` 在 HookNameSchema 中
3. 读 `src/plugin/hooks/create-session-hooks.ts`，确认 sisyphus-junior-notepad 被 safeHook 实例化
4. 确认两者不在 disabled_hooks 中

**Expected**:
- [ ] session-scorer 实例化为 `sessionScorer`，注册到 `event` handler 的 `session.stop`
- [ ] sisyphus-junior-notepad 在 HookNameSchema 中注册
- [ ] sisyphus-junior-notepad 通过 `create-session-hooks.ts` 实例化
- [ ] 两者未被禁用

**Pass**: 两个功能均已注册且可实例化
**Fail**: 任一功能注册缺失或被禁用

---

### Task V-0.1-record: 记录注册状态检查结果
将 V-0.1 结果追加到 findings 和 progress。

---

## Session 1: session-scorer 验证

### Task V-1.1: session-scorer — 单元测试 <!-- Mode: bun test -->

**Trigger**: N/A（纯评分逻辑）
**Do**: 运行 `bun test src/features/session-scorer/index.test.ts`

**Expected**:
- [x] 全部测试 PASS
- [x] 覆盖 3 个评分维度（测试覆盖、代码质量、任务完成度）
- [x] 覆盖等级划分（A/B/C/D/F/N/A）
- [x] 覆盖边界条件（0 文件、0 任务、负分 clamp）

**Pass**: 全部通过
**Fail**: 任何测试失败

---

### Task V-1.1-record: 记录 session-scorer 单元测试结果
[x] 将 V-1.1 结果追加到 findings 和 progress。

---

### Task V-1.2: session-scorer — 评分公式验证 <!-- Mode: 2 -->

**Trigger**: 静态检查 — 确认评分公式实现正确
**Do**:
1. 读 `src/features/session-scorer/index.ts`
2. 确认 3 个权重：测试覆盖 40%、代码质量 30%、任务完成度 30%
3. 确认 codeQuality 有下限 clamp（min 0）
4. 确认 Grade 映射：A≥90, B≥80, C≥70, D≥60, F<60, N/A=无数据

**Expected**:
- [x] 权重正确：0.4 + 0.3 + 0.3 = 1.0
- [x] codeQuality = 100 - lintErrors*2 - typeErrors*5, clamped to min 0
- [x] Grade 边界值正确
- [x] N/A 条件：modifiedFiles === 0 && tasksTotal === 0

**Pass**: 公式和映射完全正确
**Fail**: 权重不对、缺少 clamp、边界错误

---

### Task V-1.2-record: 记录评分公式验证结果
[x] 将 V-1.2 结果追加到 findings 和 progress。

---

### Task V-1.3: session-scorer — session.stop 事件注册 <!-- Mode: 2 -->

**Trigger**: 静态检查 — 确认 event handler 链路
**Do**:
1. 读 `src/index.ts` 中 sessionScorer 的实例化位置
2. 读 event handler 中 session.stop 分支的调用逻辑
3. 确认输出格式为 `会话质量: X (N/100)`

**Expected**:
- [x] sessionScorer 在 event handler 中被调用
- [x] 事件类型为 `session.stop`
- [x] 调用后通过 log 输出评分结果

**Pass**: 链路完整
**Fail**: sessionScorer 未在 session.stop 中被调用


**Notes**: session.stop 是会话结束事件，agent 自身无法在会话中触发。标记为 AGENT_INVISIBLE — log 输出在终端可见但不在 output.output 中。

---

### Task V-1.3-record: 记录 session.stop 事件注册结果
[x] 将 V-1.3 结果追加到 findings 和 progress。

---

## Session 2: sisyphus-junior-notepad 验证

### Task V-2.1: sisyphus-junior-notepad — 单元测试 <!-- Mode: bun test -->

**Trigger**: N/A
**Do**: 运行 `bun test src/hooks/sisyphus-junior-notepad/index.test.ts`

**Expected**:
- [x] 全部测试 PASS
- [x] 覆盖条件触发（tool === "task" && isCallerOrchestrator）
- [x] 覆盖防重复注入（已有 SYSTEM_DIRECTIVE_PREFIX 时不再注入）
- [x] 覆盖非 Atlas 调用不注入的场景

**Pass**: 全部通过
**Fail**: 任何测试失败

---

### Task V-2.1-record: 记录 sisyphus-junior-notepad 单元测试结果
[x] 将 V-2.1 结果追加到 findings 和 progress。

---

### Task V-2.2: sisyphus-junior-notepad — NOTEPAD_DIRECTIVE 内容验证 <!-- Mode: 2 -->

**Trigger**: 静态检查
**Do**:
1. 读 `src/hooks/sisyphus-junior-notepad/constants.ts`
2. 确认 NOTEPAD_DIRECTIVE 内容包含：
   - 强制使用 Edit 工具 APPEND 到 findings.md
   - 强制使用 Edit 工具 APPEND 到 progress.md
   - tasks.md 为只读（不可修改）
   - SYSTEM_DIRECTIVE_PREFIX 标记（用于防重复检测）
3. 确认指令对子代理的行为约束合理

**Expected**:
- [x] NOTEPAD_DIRECTIVE 包含 findings.md append 指令
- [x] NOTEPAD_DIRECTIVE 包含 progress.md append 指令
- [x] tasks.md 被标记为只读
- [x] SYSTEM_DIRECTIVE_PREFIX 存在且唯一

**Pass**: 指令内容完整且约束合理
**Fail**: 关键指令缺失

---

### Task V-2.2-record: 记录 NOTEPAD_DIRECTIVE 内容验证结果
[x] 将 V-2.2 结果追加到 findings 和 progress。

---

### Task V-2.3: sisyphus-junior-notepad — 条件触发链路验证 <!-- Mode: 2 -->

**Trigger**: 静态检查 — 确认 hook 的条件分支完整
**Do**:
1. 读 `src/hooks/sisyphus-junior-notepad/hook.ts`
2. 确认 3 个条件全部检查：
   - `tool === "task"`
   - `isCallerOrchestrator(input.sessionID, ctx.client)`
   - `!prompt.includes(SYSTEM_DIRECTIVE_PREFIX)`
3. 确认 args 修改方式（前置 NOTEPAD_DIRECTIVE 到 prompt 参数）

**Expected**:
- [ ] 3 个条件缺一不可（AND 逻辑）
- [ ] 条件不满足时 hook 静默返回（不报错）
- [ ] args 修改是前置（prepend）而非替换

**Pass**: 条件链路完整，args 修改安全
**Fail**: 条件逻辑有缺陷或 args 被覆盖

---

### Task V-2.3-record: 记录条件触发链路验证结果
将 V-2.3 结果追加到 findings 和 progress。

---

### Task V-2.4: sisyphus-junior-notepad — 反向触发（非 Atlas 调用） <!-- Mode: 2 反向 -->

**Trigger**: 应为 `tool.execute.before` — 但当前 agent 不是 Atlas 编排器，预期无注入
**Do**:
1. 使用 Agent 工具派发一个简单任务（当前 agent 不是 Atlas）
2. 观察子代理收到的 prompt 是否包含 NOTEPAD_DIRECTIVE

**Expected**:
- [ ] **无 NOTEPAD_DIRECTIVE 注入**（因为当前 agent 不是 Atlas 编排器）
- [ ] 子代理正常执行任务，prompt 未被修改

**Pass**: 确认非 Atlas 调用不触发注入 — 条件过滤正确
**Fail**: 非 Atlas 调用也触发了注入 — 条件检查有 bug

---

### Task V-2.4-record: 记录反向触发结果
将 V-2.4 结果追加到 findings（非 Atlas 无注入证据）和 progress。

---

## Feature Coverage Index

| V-ID | 功能 | 模式 | 来源 | 依赖 |
|------|------|------|------|------|
| V-0.1 | 注册状态检查 | 静态检查 | 前置检查 | 无 |
| V-1.1 | session-scorer 单元测试 | bun test | 50-enhancements | V-0.1 |
| V-1.2 | 评分公式验证 | Mode 2 | 50-enhancements | V-0.1 |
| V-1.3 | session.stop 事件注册 | Mode 2 (AGENT_INVISIBLE) | 50-enhancements | V-0.1 |
| V-2.1 | notepad 单元测试 | bun test | 50-enhancements | V-0.1 |
| V-2.2 | NOTEPAD_DIRECTIVE 内容 | Mode 2 | 50-enhancements | V-0.1 |
| V-2.3 | 条件触发链路 | Mode 2 | 50-enhancements | V-0.1 |
| V-2.4 | 非 Atlas 反向触发 | Mode 2 反向 | 50-enhancements | V-2.1 |
