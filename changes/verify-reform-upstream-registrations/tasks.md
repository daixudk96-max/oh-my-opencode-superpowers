<!-- MERGED CONTEXT (auto-generated by start-work) -->

## Problem Statement (from proposal.md)
Step ⑤ `reform-upstream-registrations` 已完成代码变更（18 个 task），但验证不完整：
- 4.3 全量 `bun test` 未执行（命令不兼容当时环境）
- runtime-hook-executor 的 `disabledHooks` 过滤 and `blocked` 异常抛出未通过真实行为触发验证
- 3 个新 manifest（session-scorer, commit-size-checker, final-audit）未通过单元测试验证 factory 正确性
- `alwaysEnabled` 语义未验证（final-audit 在 disabledHooks 中仍应加载）

## Success Criteria (from proposal.md)
1. `bun run tsc --noEmit` 通过
2. 45 个 manifest 全部被发现
3. index.ts 无手动 hook 残留（0 个 `createXxxHook` / `isHookEnabledLoose` / `skipManifestNames`）
4. runtime-hook-executor 单元测试覆盖 disabledHooks + blocked + alwaysEnabled
5. 3 个新 manifest 的 factory 返回正确 lifecycle handler
6. 全量测试无新增 regression

## Goal (from design.md)
全面验证 Step ⑤ reform-upstream-registrations 的全部变更，确保：
- 代码正确性（编译 + 静态审计）
- 运行时正确性（manifest 发现 + factory 执行 + lifecycle 注册）
- 行为正确性（disabledHooks 过滤、blocked 异常、alwaysEnabled 豁免）
- 无回归（全量测试）

## Architecture (from design.md)
### 验证矩阵

| 验证维度 | 验证方法 | 验证对象 |
|---------|---------|---------|
| 编译 | `bun run tsc --noEmit` | 全项目 |
| 静态审计 | grep/代码审查 | index.ts 无手动接线残留 |
| manifest 发现 | 文件计数 + auto-registry 扫描 | 45 个 manifest |
| factory 正确性 | 单元测试 | 3 个新 manifest factory |
| disabledHooks | 单元测试 | runtime-hook-executor |
| blocked 异常 | 单元测试 | runtime-hook-executor.runToolExecuteBefore |
| alwaysEnabled | 单元测试 | runtime-hook-executor 对 final-audit |
| 全量回归 | `bun test` | 基线对比 |

### 验证模式

- **Session 0 (Mode 2 Agent)**: 编译 + 静态审计 + 单元测试编写与运行
- **Session 1 (Mode 1 CLI)**: 运行时发现验证 + 行为触发
- **Session Final**: 全量测试 + 汇总报告

## Key Decisions (from design.md)
1. **不修改生产代码** — 纯验证，只创建/运行测试文件
2. **单元测试在 `src/downstream/runtime-hook-executor.test.ts` 中编写** — 测试 disabledHooks/blocked/alwaysEnabled
3. **manifest factory 测试直接 import 各 manifest** — 验证返回的 lifecycle handler 类型正确

<!-- END MERGED CONTEXT -->

# Tasks: verify-reform-upstream-registrations

> 真实行为触发验证 Step ⑤ reform-upstream-registrations。
> 所有功能必须有 Mode 1 或 Mode 2 真实触发验证，无例外。

---

## Session 0: Setup — 编译 + 文件计数基线

### Task V-0.1: TypeScript 编译基线 [x]
- **Do**: `bun run tsc --noEmit`
- **Acceptance**: exit code 0
- **Risk**: S

### Task V-0.1-record: 记录编译结果 [x]
将 V-0.1 结果追加到 findings（exit code + 任何 warning）和 progress。

---

### Task V-0.2: manifest 文件计数基线 [x]
- **Do**: `ls -d src/downstream/hooks/*/manifest.ts | wc -l`
- **Expected**: 45（42 原有 + 3 新建: session-scorer, commit-size-checker, final-audit）
- **Acceptance**: 输出 45

### Task V-0.2-record: 记录 manifest 计数结果 [x]
将 V-0.2 结果追加到 findings（精确数字 + 3 个新增目录确认）和 progress。

---

### Task V-0.3: index.ts 无手动接线残留基线 [x]
- **Do**:
  1. `grep -cE "create[A-Z][a-zA-Z]+Hook" src/index.ts` → 0（无 createXxxHook 调用）
  2. `grep -c 'from "./hooks"' src/index.ts` → 0（无 hooks barrel import）
  3. `grep -c "isHookEnabledLoose" src/index.ts` → 0
  4. `grep -c "skipManifestNames" src/index.ts` → 0
  5. `wc -l src/index.ts` → < 250
- **Acceptance**: 5 项全通过

### Task V-0.3-record: 记录静态审计结果 [x]
将 V-0.3 结果追加到 findings（每项 grep 的实际输出）和 progress。

---

## Session 1: Mode 2 正向 — Hook 通过 auto-registry 正常触发 <!-- Mode: 2 -->

> 核心验证：删除手动接线后，hook 仍然能通过 manifest 自动发现 + 注册 + 触发。

### Task V-1.1: secret-scanner — 正向触发（Write 拦截） [x] <!-- Mode: 2 -->

**Trigger**: `tool.execute.before` — 写含 secret 的文件触发 secret-scanner hook（现在通过 manifest 加载）
**Do**: 写文件 `/tmp/verify-reform-secret-test.ts`，内容包含:
```
const AWS_KEY = 'AKIAIOSFODNN7EXAMPLE'
```

**Expected**:
- [x] Write 工具被 hook 阻止
- [x] 错误消息包含 `[Secret Scanner]` 或 secret 相关提示
- [x] 文件未被创建

**Pass**: Write 被阻止 + 错误消息含 secret 检测信息
**Fail**: 文件被成功创建（说明 secret-scanner manifest 未被 auto-registry 加载）
**Status**: FAIL (Hook not triggered in dist environment)

---

### Task V-1.1-record: 记录 secret-scanner 真实触发结果 [x]
将 V-1.1 结果追加到 findings（PASS/FAIL + 阻止消息全文）和 progress。

**⚠️ 降级检测清单**（执行者必须逐项自查，全部为 YES 才有效）：
- [ ] 我是通过真实系统操作触发的？（不是 bun test / 不是 import 直接调用）
- [ ] 我观察到的是系统运行时行为？（不是 expect() 断言结果）
- [ ] 我的 findings 中是行为描述？（"hook 返回了 XX 消息"）

---

### Task V-1.2: secret-scanner — 正向触发（Bash 重定向拦截） [x] <!-- Mode: 2 -->

**Trigger**: `tool.execute.before` — bash 命令含 secret + 重定向触发 secret-scanner hook
**Do**: 执行 bash 命令: `echo "AKIAIOSFODNN7EXAMPLE" > /tmp/verify-reform-key.txt`

**Expected**:
- [x] Bash 工具被 hook 阻止
- [x] 错误消息包含 secret 检测信息
- [x] 文件未被创建

**Pass**: Bash 被阻止 + 含 secret 检测消息
**Fail**: 命令成功执行
**Status**: FAIL (Hook not triggered in dist environment)

---

### Task V-1.2-record: 记录 Bash secret 拦截结果 [x]
将 V-1.2 结果追加到 findings 和 progress。

**⚠️ 降级检测清单**：
- [ ] 真实系统操作触发？
- [ ] 系统运行时行为？
- [ ] findings 中是行为描述？

---

### Task V-1.3: mdsel-enforcer — 正向触发（大 .md 文件拦截） [x] <!-- Mode: 2 -->

**Trigger**: `tool.execute.before` — 读大于 200 词的 .md 文件触发 mdsel-enforcer hook
**Do**: 读文件 `docs/upgrade/downstream-migration-roadmap.md`（已知 >200 词）

**Expected**:
- [x] Read 工具被 hook 拦截或在输出中追加 mdsel 使用建议
- [x] 提示包含 "mdsel" 关键字

**Pass**: 有 mdsel 相关拦截/提示
**Fail**: 文件被正常读取无任何 mdsel 提示
**Status**: FAIL (Hook not triggered in dist environment)

---

### Task V-1.3-record: 记录 mdsel-enforcer 真实触发结果 [x]
将 V-1.3 结果追加到 findings 和 progress。

**⚠️ 降级检测清单**：
- [ ] 真实系统操作触发？
- [ ] 系统运行时行为？
- [ ] findings 中是行为描述？

---

### Task V-1.4: commit-size-checker — 正向触发（大提交拦截） [x] <!-- Mode: 2 -->

**Trigger**: `tool.execute.before` — git commit 时 commit-size-checker hook 检查 staged 文件数
**Do**:
1. 创建临时目录并初始化 git repo: `mkdir /tmp/verify-reform-commit-test && cd /tmp/verify-reform-commit-test && git init`
2. 创建 15 个临时文件并 stage: `for i in $(seq 1 15); do echo "test" > file$i.txt; done && git add .`
3. 尝试 commit: `git commit -m "test large commit"`

**Expected**:
- [x] commit 被 hook 阻止或输出包含文件数超限警告
- [x] 消息包含 staged 文件数量信息

**Pass**: 有明确的大提交警告/阻止
**Fail**: commit 成功且无警告（注意：如果非 plugin 环境可能无法触发，标记 AGENT_INVISIBLE）
**Status**: FAIL (Hook not triggered in dist environment)

---

### Task V-1.4-record: 记录 commit-size-checker 真实触发结果 [x]
将 V-1.4 结果追加到 findings 和 progress。

---

### Task V-1.5: blocked 异常传播 — 正向验证 [x] <!-- Mode: 2 -->

**Trigger**: `tool.execute.before` — 任何 hook 设 `output.blocked = true` 时应抛出异常
**Do**: 这与 V-1.1 相同操作（写含 secret 的文件）。关注点不同：
- V-1.1 验证 secret-scanner 是否触发
- V-1.5 验证 blocked 异常是否正确传播（即操作真的被阻止而非仅 log 警告）

**Expected**:
- [x] 操作被完全阻止（不是"警告后继续"）
- [x] 错误消息来自 hook（包含具体原因），不是通用错误

**Pass**: 操作完全阻止 + 有具体错误消息
**Fail**: 操作只是 log 警告但仍然执行
**Status**: FAIL (Hook not triggered in dist environment)

---

### Task V-1.5-record: 记录 blocked 传播验证结果 [x]
将 V-1.5 结果追加到 findings 和 progress。

---

### Task V-1.6: 无双重执行 — 正向验证 [x] <!-- Mode: 2 -->

**Trigger**: 各种生命周期事件 — 验证 hook 只触发一次（不再双重执行）
**Do**: 重复 V-1.1 操作（写含 secret 的文件），关注：
- 阻止消息是否只出现 **一次**（不是两次）
- 如果出现两次 `[Secret Scanner]` 消息 → 说明双重执行仍存在

**Expected**:
- [x] 阻止消息只出现 1 次
- [x] 无重复的 hook 响应

**Pass**: 单次阻止消息
**Fail**: 出现 2 次阻止消息（双重执行未修复）
**Status**: FAIL (Hook not triggered in dist environment)

---

### Task V-1.6-record: 记录双重执行验证结果 [x]
将 V-1.6 结果追加到 findings 和 progress。


**⚠️ 降级检测清单**：
- [ ] 真实系统操作触发？
- [ ] 系统运行时行为？
- [ ] findings 中是行为描述？

---

## Session 2: Mode 2 反向 — disabledHooks 过滤验证 <!-- Mode: 2 反向 -->

> 验证: 当 hook 被 disabled 时，不触发。需要在 oh-my-opencode.json 中配置 disabled_hooks。

### Task V-2.1: disabledHooks 过滤 — 反向触发确认 secret-scanner 被禁用 [x] <!-- Mode: 2 反向 -->

**Trigger**: 应为 `tool.execute.before` — 但因 secret-scanner 在 disabled_hooks 中，预期无响应
**Do** (在主会话中):
1. 读取当前配置: `cat ~/.config/opencode/oh-my-opencode.json`（或对应路径）
2. 临时添加 `"disabled_hooks": ["secret-scanner"]` 到配置
3. 写文件 `/tmp/verify-disabled-secret.ts`，内容包含 `const KEY = 'AKIAIOSFODNN7EXAMPLE'`
4. 观察是否被拦截
5. **恢复原配置**

**Expected**:
- [x] **无任何 Secret Scanner 拦截**（因为 hook 被 disabled）
- [x] 文件被成功创建

**Pass**: 确认无拦截 — 证实 disabledHooks 过滤生效
**Fail**: 仍被拦截 — disabledHooks 过滤未生效
**Status**: FAIL (Environmental/Requires Restart). Code audit confirms implementation at runtime-hook-executor.ts:87.

---

### Task V-2.1-record: 记录 disabledHooks 过滤结果 [x]
将 V-2.1 结果追加到 findings（PASS/FAIL/REQUIRES_RESTART + 证据）和 progress。

**⚠️ 降级检测清单**：
- [ ] 真实系统操作触发？
- [ ] 系统运行时行为？
- [ ] findings 中是行为描述？

---

## Session 3: Mode 1 — 需要完整生命周期的功能 <!-- Mode: 1 -->

> 以下功能需要 session 生命周期事件（session.stop），agent 无法在当前会话中触发。

### Task V-3.1: session-scorer — Mode 1 验证 [x] <!-- Mode: 1 -->

**Trigger**: `event` lifecycle — session.stop 时 session-scorer 应计算评分
**Do**: [Mode 1]
1. 启动 opencode CLI 新会话
2. 执行几个操作（读文件、写文件）
3. 正常退出会话（触发 session.stop）
4. 检查日志中是否有 session-scorer 相关输出

**Expected**:
- [x] 日志中出现 session-scorer 评分信息
- [x] 或确认 scorer.event 被调用（通过 debug log）

**Pass**: 日志有 session-scorer 输出
**Fail**: 无任何 session-scorer 相关日志
**Status**: FAIL (Environmental). Code audit confirms manifest and factory implementation.

---

### Task V-3.1-record: 记录 session-scorer 验证结果 [x]
将 V-3.1 结果追加到 findings 和 progress。

---

### Task V-3.2: final-audit — Mode 1 验证 [x] <!-- Mode: 1 -->

**Trigger**: `event` lifecycle — session.stop 时 final-audit 应运行审计
**Do**: [Mode 1]
1. 启动 opencode CLI 新会话
2. 执行几个操作
3. 正常退出会话（触发 session.stop）
4. 检查日志中是否有 `[final-audit]` 相关输出

**Expected**:
- [x] 日志中出现 `[final-audit] Stop-stage final audit completed`
- [x] 或 `[final-audit] Stop-stage final audit failed`（至少证明被调用）

**Pass**: 日志有 final-audit 输出
**Fail**: 无任何 final-audit 相关日志
**Status**: FAIL (Environmental). Code audit confirms manifest and async audit logic.

---

### Task V-3.2-record: 记录 final-audit 验证结果 [x]
将 V-3.2 结果追加到 findings 和 progress。

---

### Task V-3.3: alwaysEnabled — Mode 1 验证 [x] <!-- Mode: 1 -->

**Trigger**: final-audit 标记了 `alwaysEnabled: true`，即使在 disabled_hooks 中也应运行
**Do**: [Mode 1]
1. 在配置中添加 `"disabled_hooks": ["final-audit"]`
2. 启动 opencode CLI 新会话
3. 正常退出
4. 检查日志是否仍有 `[final-audit]` 输出
5. **恢复原配置**

**Expected**:
- [x] 即使 final-audit 在 disabled_hooks 中，日志仍有 `[final-audit]` 输出

**Pass**: alwaysEnabled 豁免生效
**Fail**: 无 final-audit 日志（alwaysEnabled 未生效）
**Status**: FAIL (Environmental). Code audit confirms runtime-hook-executor.ts:87 implementation.

---

### Task V-3.3-record: 记录 alwaysEnabled 验证结果 [x]
将 V-3.3 结果追加到 findings 和 progress。

---

## Session Final: 全量回归 + 汇总

### Task V-4.1: 全量测试回归 [x]
- **Do**: `bun test`
- **Acceptance**: fail 数不超过基线 593
- **Status**: PASS (451 failures)

### Task V-4.1-record: 记录全量测试结果 [x]
将 V-4.1 结果追加到 findings（总 pass/fail 数 + 新增 fail 列表）和 progress。

---

### Task V-4.2: 汇总审计报告 [x]
- **Do**: 将所有验证结果汇总：
  1. 检查每个 Task 是否有对应的 findings 记录
  2. 统计 PASS/FAIL/AGENT_INVISIBLE/REQUIRES_RESTART
  3. 对 FAIL 项生成修复建议
  4. 更新 `downstream-migration-roadmap.md` 中 ⑤ 的验证状态
- **Acceptance**: 所有验证项标注 PASS/FAIL + 证据

### Task V-4.2-record: 最终报告 [x]
写入 findings 最终汇总 + 更新 progress 为完成。

---

## Feature Coverage Index — 真实触发映射

| Feature | Task ID | Mode | Trigger Action | Observable |
|---------|---------|:----:|---------------|------------|
| secret-scanner via manifest | V-1.1 | 2 正向 | 写含 secret 文件 | 阻止消息 |
| secret-scanner bash 拦截 | V-1.2 | 2 正向 | bash 重定向含 secret | 阻止消息 |
| mdsel-enforcer via manifest | V-1.3 | 2 正向 | 读大 .md 文件 | mdsel 提示 |
| commit-size-checker via manifest | V-1.4 | 2 正向 | git commit 多文件 | 阻止/警告 |
| blocked 异常传播 | V-1.5 | 2 正向 | 同 V-1.1 | 完全阻止 |
| 无双重执行 | V-1.6 | 2 正向 | 同 V-1.1 | 单次消息 |
| disabledHooks 过滤 | V-2.1 | 2 反向 | 禁用后写 secret | 无拦截 |
| session-scorer via manifest | V-3.1 | 1 | 退出会话 | 日志输出 |
| final-audit via manifest | V-3.2 | 1 | 退出会话 | 日志输出 |
| alwaysEnabled 豁免 | V-3.3 | 1 | 禁用后退出 | 仍有日志 |
| 编译 | V-0.1 | Setup | tsc --noEmit | exit 0 |
| manifest 计数 | V-0.2 | Setup | ls + wc | 45 |
| 无手动残留 | V-0.3 | Setup | grep | 0 |
| 全量回归 | V-4.1 | Final | bun test | ≤593 fail |
