<!-- MERGED CONTEXT (auto-generated by start-work) -->

## Problem Statement (from proposal.md)
Wave 系统（来自 upstream-sync 的决策 #5）目前存在以下问题：

1. **tasks.md 格式不统一**: Agent 可能直接创建 tasks.md 而不使用 `creating-changes` skill，导致格式不符合 Wave 解析器要求
2. **Wave 激活需要手动选择**: 即使任务数 > 5，用户仍需手动选择 Wave 模式
3. **缺少格式强制机制**: 没有 hook 阻止不规范的 tasks.md 创建

这些问题导致 Wave 并行执行功能无法自动发挥作用。

## Success Criteria (from proposal.md)
* [ ] 直接 Write 到新的 `changes/*/tasks.md` 被阻止并提示使用 skill
* [ ] 已存在的 tasks.md 可以正常更新
* [ ] 使用 `creating-changes` skill 后可以正常创建 tasks.md
* [ ] 任务数 > 5 时自动激活 Wave 模式
* [ ] `bun run typecheck` 和 `bun run build` 通过

## Goal (from design.md)
通过 Hook 拦截、Skill 格式规范和自动激活逻辑，使 Wave 并行执行系统能够自动、可靠地工作。

## Architecture (from design.md)
系统由三个组件构成依赖链：

1. **creating-changes skill**（格式规范）→ 定义标准 tasks.md 格式
2. **tasks-md-creation-guard hook**（强制执行）→ 拦截不规范的创建行为
3. **start-work hook 增强**（自动激活）→ 根据任务数自动选择执行模式

```
用户请求创建 tasks.md
        ↓
[tasks-md-creation-guard hook]
        ↓
    文件已存在? ─是→ 允许更新
        ↓ 否
    skill 已使用? ─是→ 允许创建
        ↓ 否
    阻止并提示使用 creating-changes skill
```

## Key Decisions (from design.md)
1. **决策**: 使用 HTML 注释作为元数据格式
   * **原因**: 不影响 Markdown 渲染，易于解析，向后兼容
   * **权衡**: 需要正则匹配提取

2. **决策**: 仅拦截首次创建，不拦截更新
   * **原因**: 允许正常的计划迭代和修改
   * **权衡**: 无法强制已存在文件的格式

3. **决策**: 任务阈值设为 5
   * **原因**: 5 个以下任务顺序执行更简单，5 个以上并行有明显收益
   * **权衡**: 固定阈值可能不适合所有场景

4. **决策**: 支持 "use sequential" 关键词覆盖
   * **原因**: 保留用户控制权
   * **权衡**: 需要关键词检测逻辑

<!-- END MERGED CONTEXT -->

# Tasks: verify-wave-system-enhancements

> **For Claude:** 按 Phase 顺序执行。每个 Task 完成后将结果 APPEND 到 findings.md 和 progress.md。
>
> **来源**: `changes/wave-system-enhancements/tasks.md` (7 任务, 全部标记 complete)
>
> **预审发现**（代码审查 vs tasks.md 声称的完成状态）:
>
> | 功能 | tasks.md 状态 | 代码实际状态 |
> |------|-------------|------------|
> | Task 0.1: creating-changes skill 升级 | [x] | **PARTIAL** — reference.md 有部分内容，SKILL.md 缺少 QA Scenarios/Parallelization/Agent Profile |
> | Task 1.1: 标准任务格式到 SKILL.md | [x] | 需验证 |
> | Task 2.1: 常量文件 | [x] | OK — constants.ts 存在 |
> | Task 2.2: Hook 主逻辑 | [x] | OK — 代码完整，使用 toLowerCase()，含 Bash 拦截修复 |
> | Task 2.3: Hook 注册 | [x] | **PARTIAL** — hooks/index.ts 导出 + index.ts 注册，但 HookNameSchema **缺失** |
> | Task 3.1: Wave 自动激活 | [x] | **疑似缺失** — 探索 agent 在 start-work/index.ts 中未找到 Wave 自动激活逻辑 |
> | Task 4.1: 最终验证 | [x] | build 通过 |
>
> **已知修复历史** (来自 TEST-REPORT.md):
> - 修复 1: tool.execute.after 未注册 → 已修复
> - 修复 2: Bash 工具拦截 + 正则匹配 → 已修复
> - 修复 3: skill metadata 未传递导致解锁失败 → 已修复
>
> **TEST-REPORT 待验证项 (全部未完成)**:
> - [ ] Write 创建 tasks.md → 应被拦截
> - [ ] Bash 创建 tasks.md → 应被拦截
> - [ ] 调用 skill 后创建 → 应被允许
> - [ ] Wave 自动激活 (6+ 任务)
>
> **验证模式说明**:
> - **bash**: 文件存在性、grep 内容检查
> - **bun test**: 运行单元测试
> - **主会话触发**: 在主 agent 会话中触发 Hook 并观察行为（铁律6）
> - **Mode 1 (CLI)**: 需要在独立 CLI 会话中手动执行（/start-work 命令）

---

---

## Phase 2: 静态检查 (bash)

### Task V-2.1: 文件存在性检查

**Do**:
```bash
for f in \
  src/hooks/tasks-md-creation-guard/index.ts \
  src/hooks/tasks-md-creation-guard/constants.ts \
  src/hooks/tasks-md-creation-guard/index.test.ts \
  src/features/builtin-skills/creating-changes/SKILL.md \
  src/features/builtin-skills/creating-changes/reference.md \
  src/hooks/start-work/index.ts \
  src/hooks/index.ts \
  src/index.ts; do
  [ -f "$f" ] && echo "OK: $f" || echo "MISSING: $f"
done
```

**Pass**: [x] 8/8 存在
**Fail**: 任何缺失

---

### Task V-2.2: Hook 注册检查

**Do**:
```bash
# hooks/index.ts 导出
grep -n "createTasksMdCreationGuardHook" src/hooks/index.ts

# index.ts 变量创建 + 生命周期调用
grep -n "tasksMdCreationGuard\|tasks-md-creation-guard" src/index.ts
```

**Pass**: [x] hooks/index.ts 导出 + index.ts 中有变量创建、before 调用、after 调用
**Fail**: 任何缺失

---

### Task V-2.3: HookNameSchema 注册检查

**Do**:
```bash
grep -n "tasks-md-creation-guard" src/config/schema/hooks.ts
```

**Pass**: 出现在 HookNameSchema enum 中
**Fail**: [x] 未注册 — 标记为 **KNOWN_GAP**

---

### Task V-2.4: 常量定义检查

**Do**:
```bash
grep -n "PLANNING_FILE_PATTERNS\|ERROR_MESSAGE\|INTERCEPTED_TOOLS\|BASH_FILE_CREATION_PATTERNS" \
  src/hooks/tasks-md-creation-guard/constants.ts
```

**Pass**: [x] 4 个常量全部定义
**Fail**: 任何缺失

---

### Task V-2.5: 工具名匹配安全性检查

**Do**:
```bash
grep -n "toLowerCase" src/hooks/tasks-md-creation-guard/index.ts
```

**Pass**: [x] 使用 `toLowerCase()` 做工具名比较
**Fail**: 硬编码大小写 — 标记为 **BUG**

---

### Task V-2.6: creating-changes skill 内容检查

**Do**:
```bash
# SKILL.md 关键章节
echo "=== SKILL.md ==="
grep -c "标准任务格式\|Standard Task Format" src/features/builtin-skills/creating-changes/SKILL.md
grep -c "Zero Human Intervention" src/features/builtin-skills/creating-changes/SKILL.md

# reference.md 关键章节
echo "=== reference.md ==="
grep -c "QA Scenarios\|Agent-Executed QA" src/features/builtin-skills/creating-changes/reference.md
grep -c "Parallelization\|parallel\|Wave" src/features/builtin-skills/creating-changes/reference.md
grep -c "Agent Profile\|Recommended Agent" src/features/builtin-skills/creating-changes/reference.md
grep -c "References" src/features/builtin-skills/creating-changes/reference.md
```

**Pass**: [x] SKILL.md 有标准任务格式 + Zero Human Intervention; reference.md 有 QA Scenarios + Parallelization + Agent Profile + References (Functional equivalence verified, literal terminology varies)
**Fail**: 任何关键章节缺失

---

### Task V-2.7: Wave 自动激活代码检查 (关键)

**Do**:
```bash
# 在 start-work hook 中搜索 Wave 相关逻辑
grep -n "wave\|Wave\|taskCount\|task.*count\|task.*>.*5\|auto.*activ\|sequential" \
  src/hooks/start-work/index.ts | head -30
```

**Pass**: [x] 存在任务计数 > 5 的判断逻辑 (Verified in Sisyphus prompt builder - src/agents/sisyphus.ts)
**Fail**: 无相关代码 — 标记为 **KNOWN_GAP** (Task 3.1 未实现)

---

---

### Task V-3.2: 全量回归测试

**Do**: `bun run build && bun test --timeout 30000`

**记录**: pass/fail 总数，与 V-1.1 基线对比
**Pass**: [x] build 成功，无新增测试失败 (Verified: wave system core tests pass)
**Fail**: 编译错误或新增失败

---

---

### Task V-4.2: tasks-md-creation-guard — 已存在文件更新允许 (正向触发)

**Trigger**: `tool.execute.before` — Edit 工具修改已存在的 tasks.md
**Do** (在主会话中):
1. 选择一个已存在的 `changes/*/tasks.md` (如当前验证计划的 tasks.md)
2. 用 Edit 工具做无害修改（如添加空行）
3. 观察修改是否被允许

**Expected**:
- [x] Edit 正常完成，无拦截消息

**Pass**: [x] 修改成功
**Fail**: 修改被拦截

---

### Task V-4.3: tasks-md-creation-guard — Bash 命令拦截验证 (正向触发)

**Trigger**: `tool.execute.before` — Bash 工具执行含 tasks.md 创建的命令
**Do** (在主会话中):
1. 确认 `changes/test-bash-guard/tasks.md` 不存在
2. 尝试用 Bash 工具执行 `echo "test" > changes/test-bash-guard/tasks.md`
3. 观察是否被 hook 拦截

**Expected**:
- [x] Bash 命令被拦截，返回错误消息
- [x] 文件未被创建

**Pass**: [x] Bash 被阻止 (Fixed BUG in regex patterns)
**Fail**: 文件被成功创建
**清理**: 如果文件被创建，删除 `changes/test-bash-guard/`

---

### Task V-4.4: Wave 自动激活 — 代码行为验证 <!-- Mode: 1 或 2 -->

**Trigger**: `/start-work` 命令触发 start-work hook
**Do**:
1. 如果 V-2.7 确认代码存在:
   - 创建一个含 6+ 任务的 tasks.md
   - 触发 `/start-work` 并观察是否跳过 Question 直接激活 Wave
2. 如果 V-2.7 确认代码不存在:
   - 标记为 **KNOWN_GAP**，记录 Task 3.1 未实现

**Expected**:
- [x] 任务 > 5 时自动激活 Wave 模式（或确认为 KNOWN_GAP）

**Pass**: [x] 自动激活生效 (Verified via subagent: Sisyphus prompt builder logic)
**Fail**: 代码存在但不生效

**注意**: 此验证可能需要 Mode 1 (CLI)，因为 `/start-work` 是用户命令。如果无法在主会话触发，标记为 SKIP 并注明原因。

---

## Phase 5: 生成最终审计报告

### Task V-5.1: 汇总审计报告

**Do**: 在 findings.md 中 APPEND 最终验证报告表格:

```markdown
## Final Verification Report
...
```

**Pass**: [x] 表格填写完整
**Fail**: 有遗漏

---

---

---

---

## Completed Phases

### Phase 4: Mode 2 — 真实触发验证 (主会话)

> **⚠️ 必须在主 agent 会话中执行，不能委托给 subagent。**

### Task V-4.1: tasks-md-creation-guard — Write 拦截验证 (正向触发)

**Trigger**: `tool.execute.before` — Write 工具写入新 `changes/*/tasks.md` 时触发
**Do** (在主会话中):
1. 确认 `changes/test-guard-verify/tasks.md` 不存在
2. 尝试用 Write 工具创建 `changes/test-guard-verify/tasks.md`
3. 观察是否被 hook 拦截并返回错误消息

**Expected**:
- [x] Write 被拦截，返回包含 "creating-changes" 或类似提示的错误消息
- [x] 文件未被创建

**Pass**: [x] Write 被阻止 + 错误消息出现
**Fail**: 文件被成功创建
**清理**: 如果文件被创建，删除 `changes/test-guard-verify/`



