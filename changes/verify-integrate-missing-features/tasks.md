<!-- MERGED CONTEXT (auto-generated by start-work) -->

## Problem Statement (from proposal.md)
oh-my-opencode 插件与 everything-claude-code 对比后，发现 **19 项核心功能缺失**，包括：

* 持续 learning v2 系统（模式提取、知识沉淀）
* 本能模型（高频操作模式存储）
* Observer 观察者代理（后台行为审计）
* 6 个新命令（/evolve, /learn, /instinct-*, /build-fix）
* 2 个钩子（OnTaskComplete, PatternExtraction）
* 5 个领域技能（security-audit, database-optimization, backend-pattern-*）
* 2 个 MCP 服务（Memory, Sequential Thinking）

这些功能的缺失导致插件无法实现自动化学习和持续进化。

## Success Criteria (from proposal.md)
* [ ] 持续学习系统能自动提取会话中的成功模式
* [ ] 本能模型能存储、触发、更新高频操作模式
* [ ] Observer 代理能在后台监控并报告异常行为
* [ ] 6 个新命令全部可执行且功能正确
* [ ] 2 个新钩子正确触发并提取模式
* [ ] 5 个领域技能可通过 `skill()` 调用
* [ ] Memory MCP 能跨会话保持记忆
* [ ] `bun run build` 成功
* [ ] `bun test` 全部通过（新增测试覆盖率 > 80%）
* [ ] 无新增 `as any` 或 `@ts-ignore`

## Goal (from design.md)
实现 oh-my-opencode 插件的 19 项缺失功能，建立完整的自动化学习和持续进化能力。

## Architecture (from design.md)
### 本能系统设计（关键变更）

**本能不是独立的存储系统，而是通过正式 skill 创建流程生成的完整技能。**

| 原设计（everything-claude-code）           | 适配后的设计（oh-my-opencode）                    |
| :------------------------------------ | :---------------------------------------- |
| 本能是独立的 .md 文件                         | 本能通过 **skill-create-and-change** 流程生成完整技能 |
| 存储在 `~/.claude/homunculus/instincts/` | 存储在 `~/.claude/skills/instincts/`         |
| 使用自定义脚本创建                             | 调用 `skill("skill-create-and-change")` 创建  |
| 使用自定义逻辑读取                             | 通过现有 skill 加载机制读取                         |
| 使用自定义备份                               | 调用 `skill("skill-backup")` 备份             |

### 本能工作流程

```
┌─────────────────────────────────────────────────────────────────┐
│                     本能生命周期                                  │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  [观察阶段 - PostToolUse 钩子]                                    │
│                                                                  │
│  工具调用 → observe.sh 脚本 → observations.jsonl                  │
│                                                                  │
│           │ 达到阈值时触发                                        │
│           ▼                                                      │
│                                                                  │
│  [分析阶段 - Observer 代理]                                       │
│                                                                  │
│  后台 Observer 代理分析观察日志                                    │
│    ├── 检测用户纠正                                              │
│    ├── 检测错误修复                                              │
│    ├── 检测重复工作流                                            │
│    └── 检测工具偏好                                              │
│                                                                  │
│           │ 检测到模式时                                          │
│           ▼                                                      │
│                                                                  │
│  [创建阶段 - 调用 skill-create-and-change]                       │
│                                                                  │
│  调用 skill("skill-create-and-change") 创建正式技能               │
│    → 生成完整目录结构                                             │
│    → 存储到 ~/.claude/skills/instincts/{instinct-name}/          │
│    → SKILL.md 包含 trigger/confidence/action/evidence            │
│                                                                  │
│           │ 使用时                                                │
│           ▼                                                      │
│                                                                  │
│  [触发阶段 - PreToolUse 钩子]                                     │
│                                                                  │
│  用户输入 → 匹配 instincts/ 下的 skill → 注入建议                  │
│                                                                  │
│           │ /evolve 命令时                                        │
│           ▼                                                      │
│                                                                  │
│  [进化阶段 - 聚类生成新技能]                                       │
│                                                                  │
│  调用 progressive-disclosure-md 提取相关本能                      │
│    → 聚类相似本能                                                 │
│    → 调用 skill-create-and-change 生成 evolved skill              │
│    → 调用 skill-backup 备份                                       │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

### 本能 Skill 格式

每个本能是一个完整的 skill 目录：

```
~/.claude/skills/instincts/
├── prefer-functional-style/
│   └── SKILL.md
├── always-test-first/
│   └── SKILL.md
└── use-zod-validation/
    └── SKILL.md
```

**SKILL.md 格式（复制自 everything-claude-code）：**

```yaml
---
name: prefer-functional-style
description: "When writing new functions, prefer functional patterns"
trigger: "when writing new functions"
confidence: 0.7
domain: "code-style"
source: "session-observation"
instinct: true  # 标记为本能类型的 skill
---

# Prefer Functional Style

## Action
Use functional patterns over classes when appropriate.

## Evidence
- Observed 5 instances of functional pattern preference
- User corrected class-based approach to functional on 2025-01-28
```

### 存储架构

```
~/.claude/
├── homunculus/
│   └── observations.jsonl       # 工具调用观察日志（复制自 everything-claude-code）
│
├── skills/
│   └── instincts/               # 本能存储（作为正式 skill）
│       ├── prefer-functional-style/
│       │   └── SKILL.md
│       ├── always-test-first/
│       │   └── SKILL.md
│       └── ...
│
└── skill-snapshots/             # skill-backup 的存储位置
```

### Observer 代理设计

采用**混合模式**（复制自 everything-claude-code）：

| 层级           | 触发时机                              | 实现方式                                                              | 用途              |
| :----------- | :-------------------------------- | :---------------------------------------------------------------- | :-------------- |
| **L1: 轻量检测** | 每次 PostToolUse                    | 钩子内同步检测                                                           | 检测异常模式（循环、重复失败） |
| **L2: 定期分析** | 每 20 次工具调用                        | `delegate_task(subagent_type="observer", run_in_background=true)` | 深度模式分析          |
| **L3: 会话总结** | `session.deleted` 或 `onSummarize` | 同步处理                                                              | 完整模式提取和本能生成     |

**Observer 代理配置：**

* 默认模型：`Antigravity-Gemini/gemini-3-flash`
* 可在 `~/.config/opencode/oh-my-opencode.json` 中覆盖
* 需要添加到 `OverridableAgentNameSchema`

### 数据流

1. **观察流程**:
   * `PostToolUse` → observe.sh → observations.jsonl

2. **分析流程**:
   * Observer 代理读取 observations.jsonl → 检测模式 → 调用 skill-create-and-change 创建本能 skill

3. **触发流程**:
   * 用户输入 → `PreToolUse` → 扫描 `~/.claude/skills/instincts/` → 匹配 trigger → 注入 Action

4. **进化流程**:
   * `/evolve` → 读取所有本能 skill → 聚类 → 调用 skill-create-and-change 生成新 skill

## Key Decisions (from design.md)
### 1. 本能存储在 `~/.claude/skills/instincts/`

* **Why**: 与 everything-claude-code 保持一致，复用现有 skill 加载机制
* **Trade-off**: 需要确保 skill 加载器扫描此目录

### 2. 通过 skill-create-and-change 创建本能

* **Why**: 复用现有基础设施，保持一致性
* **Trade-off**: 创建过程可能较慢，但更可靠

### 3. Observer 使用 gemini-3-flash

* **Why**: 轻量级、快速、成本低
* **Trade-off**: 分析能力有限，复杂模式可能漏检

### 4. 本能 SKILL.md 包含 `instinct: true` 标记

* **Why**: 区分本能 skill 和普通 skill
* **Trade-off**: 需要在 skill 加载器中处理此标记

<!-- END MERGED CONTEXT -->

# Tasks: verify-integrate-missing-features

> **For Claude:** 按 Phase 顺序执行。每个 Task 完成后将结果 APPEND 到 findings.md 和 progress.md。
>
> **来源**: `changes/integrate-missing-features/tasks.md` (8 任务, 3 个 Phase)
>
> **预审发现**（代码审查 vs tasks.md 内容）:
>
> | Task | 功能 | 代码状态 | 注册到 index.ts | HookNameSchema | 实际使用 |
> |------|------|---------|:--------------:|:--------------:|:--------:|
> | 1.1 | Skill Auto-Injector 注册 | OK | YES (L205,274,301) | **MISSING** | chat.message + event |
> | 1.2 | MCP Health Checker 注册 | OK | **PARTIAL** | N/A | checkAllOnStartup 未调用 |
> | 1.3 | Commit Size Checker 注册 | OK | YES (L233,360) | **MISSING** | tool.execute.before |
> | 1.4 | Context Detector 集成 | OK | YES (L84,100) | N/A | 条件匹配已生效 |
> | 2.1 | TDD State Tracker 集成 | OK | **NO** | N/A | index.ts 用本地 Map 替代 |
> | 2.2 | Template Generator 集成 | OK | YES (L22,118) | N/A | 已正确导入使用 |
> | 3.1 | Agent Chains 集成 | OK | YES (commands.ts L5,296,309) | N/A | 已正确集成 |
> | 3.2 | Dead Code Detector 集成 | OK | YES (refactor.ts L490) | N/A | 已正确集成 |
>
> **总结**: 8/8 任务代码已实现。关键问题：
> - **HIGH**: MCP Health Checker 的 `checkAllOnStartup` 未在启动时调用
> - **HIGH**: TDD State Tracker 未被 TDD Guard 实际使用（用本地 Map 替代）
> - **KNOWN_GAP**: skill-auto-injector、commit-size-checker 未注册到 HookNameSchema（使用 isHookEnabledLoose 绕过）
>
> **本变更包含 3 类功能**:
> - **A**: Hook 注册 — skill-auto-injector, MCP health, commit-size-checker, context-detector (4 项)
> - **B**: TDD Guard 增强 — state-tracker, template-generator (2 项)
> - **C**: 命令增强 — agent-chains, dead-code-detector (2 项)

---

## Phase 1: 构建基线

### Task V-1.1: 构建 + 基线测试 [x]

**Do**:
```bash
bun run build
bun test --timeout 30000 2>&1 | tail -5
```

**记录**: 编译是否成功 + 测试 pass/fail 数
**Pass**: 编译成功
**Fail**: 编译失败

---

## Phase 2: 静态检查 — 功能 A (Hook 注册)

### Task V-2.1: Skill Auto-Injector 注册 + 生命周期 [x]

**Do**:
```bash
grep -n "skillAutoInjector\|skill-auto-injector\|SkillAutoInjector" src/index.ts
```

**Pass**: 有变量创建 + chat.message 调用 + event 调用
**Fail**: 任何缺失

---

### Task V-2.2: MCP Health Checker 启动调用 [x] (BUG)

**Do**:
```bash
# 检查 health-checker.ts 是否存在
ls src/mcp/health-checker.ts

# 检查 checkAllOnStartup 是否在 index.ts 中被调用
grep -n "checkAllOnStartup\|mcpHealth\|health-checker" src/index.ts

# 检查 mcp/index.ts 中的导出情况
grep -n "checkAllOnStartup\|healthChecker\|health" src/mcp/index.ts
```

**Pass**: checkAllOnStartup 在启动流程中被调用
**Fail**: checkAllOnStartup 未被调用 — 标记 **BUG**

---

### Task V-2.3: Commit Size Checker 注册 + 生命周期 [x]

**Do**:
```bash
grep -n "commitSize\|commit-size\|CommitSize" src/index.ts
```

**Pass**: 有变量创建 + tool.execute.before 中拦截 bash 工具
**Fail**: 任何缺失

---

### Task V-2.4: Context Detector 集成验证 [x]

**Do**:
```bash
# 检查 context-detector 是否在 index.ts 中使用
grep -n "contextDetector\|context-detector\|matchesCondition\|ContextDetector" src/index.ts

# 检查是否有 hook 使用 when 条件
grep -rn "when:" src/hooks/*/index.ts 2>/dev/null | head -5
```

**Pass**: ContextDetector 初始化 + 在 hook 执行逻辑中检查条件
**Fail**: 未集成

---

## Phase 3: 静态检查 — 功能 B (TDD Guard 增强)

### Task V-3.1: TDD State Tracker 集成状态 [x] (DEAD_CODE)

**Do**:
```bash
# 检查 state-tracker.ts 是否存在
ls src/hooks/tdd-guard/state-tracker.ts

# 检查是否在 tdd-guard/index.ts 中被导入使用
grep -n "stateTracker\|state-tracker\|TddStateTracker\|updateFromTestResults" \
  src/hooks/tdd-guard/index.ts

# 检查 tdd-guard/index.ts 用什么做状态管理
grep -n "new Map\|new Set\|testResults\|tddState" src/hooks/tdd-guard/index.ts | head -10
```

**Pass**: TddStateTracker 被导入并实际使用
**Fail**: 文件存在但未被使用（死代码）— 标记 **DEAD_CODE**

---

### Task V-3.2: Template Generator 集成验证 [x]

**Do**:
```bash
grep -n "templateGenerator\|template-generator\|TemplateGenerator\|generateTestTemplate" \
  src/hooks/tdd-guard/index.ts
```

**Pass**: TemplateGenerator 被导入 + 在 tool.execute.after 中调用
**Fail**: 未集成

---

## Phase 4: 静态检查 — 功能 C (命令增强)

### Task V-4.1: Agent Chains 集成到 Sisyphus [x]

**Do**:
```bash
# 检查 agent-chains.ts 是否存在
ls src/features/builtin-commands/templates/agent-chains.ts

# 检查是否在 commands.ts 中注册
grep -n "agentChains\|agent-chains\|AgentChains" \
  src/features/builtin-commands/commands.ts

# 检查是否在 sisyphus 相关文件中引用
grep -rn "agent-chains\|agentChains\|bugfix.*chain\|refactor.*chain" src/agents/ | head -5
```

**Pass**: agent-chains 被导入且通过命令系统可访问
**Fail**: 未集成

---

### Task V-4.2: Dead Code Detector 集成到 /refactor [x]

**Do**:
```bash
# 检查 dead-code-detector.ts 是否存在
ls src/features/builtin-commands/templates/dead-code-detector.ts

# 检查是否在 refactor.ts 中被使用
grep -n "deadCode\|dead-code\|DeadCode\|createDeadCodeDetector" \
  src/features/builtin-commands/templates/refactor.ts
```

**Pass**: DeadCodeDetector 在 refactor.ts 中被导入并调用
**Fail**: 未集成

---

## Phase 5: 单元测试

### Task V-5.1: 各模块单元测试 [x]

**Do**:
```bash
bun test src/hooks/skill-auto-injector/ --timeout 30000
bun test src/mcp/health-checker.test.ts --timeout 30000
bun test src/hooks/pre-tool-use/commit-size-checker.test.ts --timeout 30000
bun test src/shared/context-detector.test.ts --timeout 30000
bun test src/hooks/tdd-guard/state-tracker.test.ts --timeout 30000
bun test src/hooks/tdd-guard/template-generator.test.ts --timeout 30000
bun test src/features/builtin-commands/templates/agent-chains.test.ts --timeout 30000
bun test src/features/builtin-commands/templates/dead-code-detector.test.ts --timeout 30000
```

**记录**: 每个模块的 pass/fail
**Pass**: 全部通过
**Fail**: 任何失败 — 标记具体模块

---

### Task V-5.2: 全量回归测试 [x] (Regression FAIL)

**Do**: `bun run build && bun test --timeout 30000`

**记录**: pass/fail 总数，与 V-1.1 基线对比
**Pass**: 无新增失败
**Fail**: 新增失败

---

## Phase 6: Mode 2 — 真实触发验证

> **⚠️ 部分任务必须在主 agent 会话中执行。**

### Task V-6.1: Skill Auto-Injector — 聊天消息触发 [x]

**Trigger**: `chat.message` — 用户发送包含技能关键词的消息
**Do**:
1. 发送包含 "git commit" 或 "database" 的消息
2. 观察是否有 skill 被自动注入到上下文

**Expected**:
- [ ] 相关 skill 提示出现在响应中

**Pass**: skill 注入逻辑触发
**Fail**: 完全无效果

---

### Task V-6.2: Commit Size Checker — 大提交拦截 (正向触发) [x] (Logic OK, UI Silent)

**Trigger**: `tool.execute.before` — 检测 `git commit` 命令
**Do**:
1. 暂存 4+ 个文件
2. 执行 `git commit`
3. 观察是否有大小警告

**Expected**:
- [ ] 显示 `[Commit Size]` 相关警告

**Pass**: 大提交被检测到
**Fail**: 无警告

---

### Task V-6.3: Commit Size Checker — 小提交不触发 (反向触发) [x]

**Trigger**: `tool.execute.before`
**Do**:
1. 暂存 1-3 个文件
2. 执行 `git commit`

**Expected**:
- [ ] 正常提交，无警告

**Pass**: 小提交不触发
**Fail**: 误报

---

### Task V-6.4: Template Generator — 新文件自动生成测试模板 [x]

**Trigger**: `tool.execute.after` — 检测新建实现文件
**Do**:
1. 创建一个新的 `.ts` 文件（非测试文件）
2. 观察是否自动生成对应的 `.test.ts` 模板

**Expected**:
- [ ] 生成测试模板或显示提示

**Pass**: 测试模板生成逻辑触发
**Fail**: 无反应

---

## Phase 7: HookNameSchema 检查

### Task V-7.1: HookNameSchema 批量检查 [x] (KNOWN_GAP)

**Do**:
```bash
for h in skill-auto-injector commit-size-checker tdd-guard; do
  found=$(grep -c "\"$h\"" src/config/schema/hooks.ts)
  echo "$h: $found"
done
```

**Pass**: 全部在 schema 中
**Fail**: 任何缺失 — 标记 **KNOWN_GAP**

---

## Phase 8: 生成最终审计报告

### Task V-8.1: 汇总审计报告 [x]

**Do**: 在 findings.md 中 APPEND 最终验证报告表格

**Pass**: 表格填写完整
**Fail**: 有遗漏
