<!-- MERGED CONTEXT (auto-generated by start-work) -->

## Problem Statement (from proposal.md)
`test-hooks-real-environment` 引入了 3 个 hook 功能，之前的验证（2026-01-24）发现部分未触发。
代码库已经过重构（plugin 架构），3 个 hook 均已接入生命周期，但需要在新架构下重新验证。

## Success Criteria (from proposal.md)
* 3 个 hook 全部有 PASS/FAIL 结论 + 可观察证据
* 发现的问题记录到 findings.md
* 之前的已知问题（comment-checker CLI 异步、directory-readme-injector 子目录限制）验证是否已修复

## Goal (from design.md)
验证 test-hooks-real-environment 引入的 3 个 hook 在新 plugin 架构下的真实运行时行为。

## Architecture (from design.md)
```
src/hooks/index.ts (导出)
  ↓
src/plugin/hooks/create-tool-guard-hooks.ts (实例化 comment-checker, directory-agents-injector)
src/plugin/hooks/create-continuation-hooks.ts (实例化 background-notification)
  ↓
src/plugin/tool-execute-before.ts (tool.execute.before 生命周期)
src/plugin/tool-execute-after.ts  (tool.execute.after 生命周期)
src/plugin/chat-message.ts        (chat.message 生命周期)
src/plugin/event.ts               (event 生命周期)
```

## Key Decisions (from design.md)
1. 3 个 hook 全部用 Mode 2 正向触发（代码已接入生命周期）
2. comment-checker 额外跑单元测试验证 CLI 层逻辑
3. directory-agents-injector 需要先确认 native support 检测是否禁用了 hook

<!-- END MERGED CONTEXT -->

# Tasks: verify-test-hooks-real-environment3

## 概述

- **目标**: 验证 test-hooks-real-environment 引入的 3 个 hook 在新 plugin 架构下的真实行为
- **功能数**: 3 个 hook
- **Task 数**: 8 个 verify + 8 个 record = 16 个 task
- **验证模式**: Mode 2 正向触发 + bun test

---

## Session 0: 前置检查

### Task V-0.1: 确认 3 个 hook 的启用状态 [x]
### Task V-0.1-record: 记录启用状态检查结果 [x]
将 V-0.1 结果追加到 findings（配置详情）和 progress（状态 + 时间戳）。

---

## Session 1: background-notification 验证

### Task V-1.1: background-notification — 单元测试 [x]
### Task V-1.1-record: 记录 background-notification 单元测试结果 [x]
将 V-1.1 结果追加到 findings 和 progress。

---

### Task V-1.2: background-notification — 正向触发（后台任务通知） [x]
### Task V-1.2-record: 记录 background-notification 正向触发结果 [x]
将 V-1.2 结果追加到 findings（通知内容截取）和 progress。

---

## Session 2: directory-agents-injector 验证

### Task V-2.1: directory-agents-injector — 单元测试 [x]
### Task V-2.1-record: 记录 directory-agents-injector 单元测试结果 [x]
将 V-2.1 结果追加到 findings 和 progress。

---

### Task V-2.2: directory-agents-injector — 正向触发（读文件注入 AGENTS.md） [x]
### Task V-2.2-record: 记录 directory-agents-injector 正向触发结果 [x]
将 V-2.2 结果追加到 findings（注入内容）和 progress。

---

### Task V-2.3: directory-agents-injector — native support 检测 [x]
### Task V-2.3-record: 记录 native support 检测结果 [x]
将 V-2.3 结果追加到 findings 和 progress。

---

## Session 3: comment-checker 验证

### Task V-3.1: comment-checker — 单元测试 [x]
### Task V-3.1-record: 记录 comment-checker 单元测试结果 [x]
将 V-3.1 结果追加到 findings 和 progress。

---

### Task V-3.2: comment-checker — 正向触发（写入带注释代码） [x]
### Task V-3.2-record: 记录 comment-checker 正向触发结果 [x]
将 V-3.2 结果追加到 findings（警告内容或失败原因）和 progress。

---

### Task V-3.3: comment-checker — CLI 二进制检查 [x]
### Task V-3.3-record: 记录 CLI 二进制检查结果 [x]
将 V-3.3 结果追加到 findings 和 progress。

---

## Feature Coverage Index

| V-ID | 功能 | 模式 | 来源 | 依赖 |
|------|------|------|------|------|
| V-0.1 | 3 hook 启用状态 | Mode 2 | 前置检查 | 无 |
| V-1.1 | background-notification 单元测试 | bun test | test-hooks-real-env | V-0.1 |
| V-1.2 | background-notification 正向触发 | Mode 2 | test-hooks-real-env | V-0.1 |
| V-2.1 | directory-agents-injector 单元测试 | bun test | test-hooks-real-env | V-0.1 |
| V-2.2 | directory-agents-injector 正向触发 | Mode 2 | test-hooks-real-env | V-0.1 |
| V-2.3 | directory-agents-injector native support | Mode 2 | test-hooks-real-env | V-0.1 |
| V-3.1 | comment-checker 单元测试 | bun test | test-hooks-real-env | V-0.1 |
| V-3.2 | comment-checker 正向触发 | Mode 2 | test-hooks-real-env | V-0.1 |
| V-3.3 | comment-checker CLI 二进制 | Mode 2 | test-hooks-real-env | V-0.1 |
