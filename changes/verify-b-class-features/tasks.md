<!-- MERGED CONTEXT (auto-generated by start-work) -->

## Problem Statement (from proposal.md)
路线图 ①~⑥ 已全部完成，dist 静态注册修复也已就绪（63 manifest 打包成功）。但 41 个 B 类功能（代码存在 + manifest 已注册）从未经过真实行为触发验证。我们无法确认这些功能在 dist 生产环境下是否真正生效。

此前 ⑤ 的验证明确暴露了 auto-registry 在 dist 下全面失效的问题（已通过 `generate-registry.ts` 静态注册修复），但修复后尚未在真实 CLI 中确认过任何一个下游 hook/skill/command 能正常触发。

## Success Criteria (from proposal.md)
* [ ] 41 个 B 类功能全部标注验证状态（PASS/FAIL/AGENT_INVISIBLE）
* [ ] dist 环境下至少 1 个 hook 成功拦截（证明静态注册修复有效）
* [ ] 全量 bun test 无新增 regression（与 ⑥ 基线对比）
* [ ] findings.md 包含每个功能的验证证据

## Goal (from design.md)
验证 41 个 B 类功能在 dist 生产环境下能正常工作，确认静态注册修复有效。

## Architecture (from design.md)
分层验证架构：静态验证 → 单元测试 → 真实行为触发 → 汇总回归。

每个功能经过三级验证筛选：

1. **Level 0 (静态)**: manifest 存在 + tsc 编译通过 + 被 generated-registry.ts 包含
2. **Level 1 (单元测试)**: 相关 .test.ts 通过（如有）
3. **Level 2 (行为触发)**: 在 dist CLI 中触发实际行为，观察输出

### 验证矩阵

| 类别       |  数量 |   Level 0   |   Level 1   |   Level 2   |
| -------- | :-: | :---------: | :---------: | :---------: |
| Hooks    |  28 | manifest 检查 |   .test.ts  |  agent 操作触发 |
| Skills   |  8  | manifest 检查 | SKILL.md 加载 | /skill 命令调用 |
| Commands |  6  | manifest 检查 |     模板加载    | /command 执行 |
| Modules  |  4  |     文件存在    |   .test.ts  |    相关功能调用   |

### 验证模式说明

| Session |  Mode | 执行者          | 可观察渠道                |
| ------- | :---: | ------------ | -------------------- |
| 0       | Setup | agent        | CLI 输出 (tsc/grep/wc) |
| 1       |  2 正向 | agent (当前会话) | hook 阻止消息 / 工具输出追加   |
| 2       |   汇总  | agent        | bun test 输出          |

## Key Decisions (from design.md)
1. **Decision**: 不修改生产代码
   * **Why**: 纯验证阶段，避免引入新风险
   * **Trade-off**: 发现问题只记录不修复，修复留给下一个 changes

2. **Decision**: 按类别分组验证而非逐一验证
   * **Why**: Hooks 触发模式相似，可以批量验证（如所有 PreToolUse hooks 用一个操作同时触发）
   * **Trade-off**: 个别 hook 可能被遗漏，需要补充验证

3. **Decision**: AGENT_INVISIBLE 作为合法验证状态
   * **Why**: 部分 hook（如 session-scorer, final-audit）只在 session 结束时触发，agent 无法观察
   * **Trade-off**: 这些功能需要人工在真实 CLI 中验证

<!-- END MERGED CONTEXT -->

# Tasks: verify-b-class-features

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

---

## Session 0: Setup — 静态基线确立

### Task 0.1: 编译 + 构建基线 <!-- Risk: Tier-0 -->

**Description:**
确认 tsc 零错误 + bun run build 成功 + generated-registry.ts 包含全部 63 manifest。

**Files:**
- Verify: `src/downstream/generated-registry.ts`
- Verify: `dist/index.js`

**Acceptance Criteria:**
- [x] `bun run tsc --noEmit` 零错误
- [x] `bun run build` 成功输出 dist/index.js
- [x] `generated-registry.ts` 包含 45 hooks + 9 skills + 6 commands + 3 agents = 63 manifest import

**Dependencies:** None

---

### Task 0.2: B 类 Hook manifest 审计 <!-- Risk: Tier-0 -->

**Description:**
确认全部 28 个 B 类 Hook 的 manifest 都存在于 generated-registry.ts 中。

**Files:**
- Verify: `src/downstream/generated-registry.ts`
- Verify: `src/downstream/hooks/*/manifest.ts` (28 个目录)

**Acceptance Criteria:**
- [x] 列出全部 28 个 B 类 Hook 名称
- [x] 每个在 generated-registry.ts 中有对应 import 行
- [x] 每个 manifest.ts 的 lifecycle 字段非空

**Dependencies:** Task 0.1

---

### Task 0.3: B 类 Skill/Command/Module 审计 <!-- Risk: Tier-0 -->

**Description:**
确认 8 个 Skills、6 个 Commands、3 个 Agents 的 manifest 存在。确认 4 个独立模块文件存在。

**Files:**
- Verify: `src/downstream/skills/*/manifest.ts` (8 个)
- Verify: `src/downstream/commands/*/manifest.ts` (6 个)
- Verify: `src/downstream/agents/*/manifest.ts` (3 个)
- Verify: `src/shared/skill-reminder-generator.ts`
- Verify: `src/mcp/health-checker.ts`
- Verify: `src/features/boulder-state/worktree-manager.ts`
- Verify: `src/features/builtin-skills/mdsel/cli.mjs`

**Acceptance Criteria:**
- [x] 8 skill manifests 在 generated-registry.ts 中有 import
- [x] 6 command manifests 在 generated-registry.ts 中有 import
- [x] 3 agent manifests 在 generated-registry.ts 中 have import
- [x] 4 个独立模块文件存在且非空

**Dependencies:** Task 0.1

---

### Task 0.4: 单元测试扫描 <!-- Risk: Tier-0 -->

**Description:**
扫描所有 B 类功能对应的 .test.ts 文件，确认哪些有测试、哪些没有。

**Files:**
- Scan: `src/hooks/*/index.test.ts` 或 `*.test.ts`
- Scan: `src/mcp/health-checker.test.ts`
- Scan: `src/features/boulder-state/worktree-manager.test.ts`

**Acceptance Criteria:**
- [x] 生成一张表格：功能名 | 有测试文件 | 测试数量
- [x] 记录到 findings.md

**Dependencies:** None

---

## Session 1: 真实行为触发验证

### Task 1.1: dist 环境 hook 加载验证 <!-- Risk: Tier-2 -->

**Description:**
在 dist 构建的 CLI 中启动一个会话，检查 auto-registry 是否成功加载了下游 hooks。这是最关键的验证——确认静态注册修复真正有效。

**Files:**
- Verify: `dist/index.js`

**Acceptance Criteria:**
- [x] 启动 dist CLI 后，auto-registry 日志显示加载了 >40 个 hooks
- [x] 或通过 grep dist/index.js 确认 hook factory 函数被引用

**TDD Test Cases:**
1. **Test**: dist 包含 secret-scanner hook factory
   - **Given**: `dist/index.js` 已构建
   - **When**: `grep "secret-scanner" dist/index.js`
   - **Then**: 返回 >0 个匹配

2. **Test**: dist 包含 mdsel-enforcer hook factory
   - **Given**: `dist/index.js` 已构建
   - **When**: `grep "mdsel-enforcer" dist/index.js`
   - **Then**: 返回 >0 个匹配

**Dependencies:** Task 0.1

---

### Task 1.2: PreToolUse Hook 批量触发 <!-- Risk: Tier-2 -->

**Description:**
触发需要 PreToolUse lifecycle 的 B 类 hooks。通过 agent 执行文件写入/读取操作，观察 hook 是否产生了拦截 or 注入行为。

涉及 hooks: secret-scanner, mdsel-enforcer, write-existing-file-guard, observation-write-guard, notepad-write-guard, commit-size-checker, comment-checker

**Acceptance Criteria:**
- [x] 写入含敏感信息的文件 → secret-scanner 拦截（PASS/FAIL）
- [x] 写入大 .md 文件 → mdsel-enforcer 触发提醒（PASS/FAIL/AGENT_INVISIBLE）
- [x] 写入已有文件 → write-existing-file-guard 检查（PASS/FAIL/AGENT_INVISIBLE）
- [x] 每个 hook 的结果记录到 findings.md

**Dependencies:** Task 1.1

---

### Task 1.3: PostToolUse Hook 批量触发 <!-- Risk: Tier-2 -->

**Description:**
触发 PostToolUse lifecycle 的 B 类 hooks。通过 agent 完成工具操作后观察后处理行为。

涉及 hooks: observation-recorder, codebase-assessment, debugging-injector, failure-counter, behavior-anchor, verbosity-controller

**Acceptance Criteria:**
- [x] 执行操作后观察是否有额外输出注入
- [x] 检查 failure-counter 是否在错误后递增
- [x] 每个 hook 的结果记录到 findings.md

**Dependencies:** Task 1.1

---

### Task 1.4: Notification Hook 触发 <!-- Risk: Tier-2 -->

**Description:**
触发 Notification lifecycle 的 hooks。这些 hooks 在 assistant 消息后触发。

涉及 hooks: plan-attention-refresher, plan-update-reminder, phase-flow-enforcer, phase-rules-injector, planning-flow-guide, skill-suggestion, project-context-injector, pr-context-injector, knowledge-injection, skill-auto-injector

**Acceptance Criteria:**
- [x] 在有活跃 plan 的状态下操作，观察 plan-attention-refresher 是否注入提醒
- [x] 检查 skill-suggestion 是否在相关场景建议 skill
- [x] 标注每个 hook 为 PASS/FAIL/AGENT_INVISIBLE

**Dependencies:** Task 1.1

---

### Task 1.5: Skill 加载验证 <!-- Risk: Tier-1 -->

**Description:**
验证 8 个 B 类 Skills 能正确加载和触发。

涉及 skills: backend-pattern-go, backend-pattern-java, backend-pattern-python, continuous-learning, database-optimization, wave-parallel-execution, mdsel, progressive-disclosure-md

**Acceptance Criteria:**
- [x] 每个 skill 的 SKILL.md 可被 skill loader 发现
- [x] mdsel skill 可通过 /mdsel 触发（有 .md 文件时）
- [x] progressive-disclosure-md 可通过触发词激活
- [x] 标注每个 skill 为 PASS/FAIL/AGENT_INVISIBLE

**Dependencies:** Task 1.1

---

### Task 1.6: Command 执行验证 <!-- Risk: Tier-1 -->

**Description:**
验证 6 个 B 类 Commands 能正确加载和执行。

涉及 commands: /evolve, /learn, /instinct-status, /instinct-import, /instinct-export, /build-fix

**Acceptance Criteria:**
- [x] 每个 command 在 help 中可见
- [x] /build-fix 可被调用（即使无构建错误也应有输出）
- [x] /instinct-status 可被调用（即使无 instinct 数据也应有提示）
- [x] 标注每个 command 为 PASS/FAIL/AGENT_INVISIBLE

**Dependencies:** Task 1.1

---

### Task 1.7: 独立模块验证 <!-- Risk: Tier-1 -->

**Description:**
验证 4 个独立模块的核心功能可用。

涉及模块:
- skill-reminder-generator: 生成 skill 提醒文本
- MCP Health Checker: `checkAllOnStartup()` 可调用
- mdsel CLI bundle: `cli.mjs` 可执行
- worktree-manager: Git worktree 创建/清理

**Acceptance Criteria:**
- [x] skill-reminder-generator 的导出函数存在且可调用
- [x] MCP Health Checker test 通过
- [x] mdsel cli.mjs 可通过 `node cli.mjs --help` 执行
- [x] worktree-manager 的导出函数存在

**Dependencies:** Task 0.3

---

## Session 2: 汇总 + 回归

### Task 2.1: 全量测试回归 <!-- Risk: Tier-2 -->

**Description:**
运行 `bun test` 全量测试，与 ⑥ 基线对比确认无新增 regression。

**Acceptance Criteria:**
- [ ] `bun test` 完成
- [ ] fail 数量 ≤ ⑥ 基线（约 451）
- [ ] 无新增 fail 类别

**Dependencies:** Task 1.1~1.7

---

### Task 2.2: 汇总验证报告 <!-- Risk: Tier-0 -->

**Description:**
汇总所有验证结果，更新 findings.md 和路线图。

**Files:**
- Modify: `changes/verify-b-class-features/findings.md`
- Modify: `docs/upgrade/downstream-migration-roadmap.md`

**Acceptance Criteria:**
- [ ] findings.md 包含 41 个功能的完整验证结果表
- [ ] 路线图 B 类统计更新为实际验证结果
- [ ] 标注下一步行动（哪些 FAIL 需要修复）

**Dependencies:** Task 2.1

---

## Legend

- `[ ]` = Pending
- `[x]` = Complete
- `[~]` = In Progress
- `[-]` = Skipped

## Risk Tiers

| Tier | Description | TDD Requirement |
|------|-------------|-----------------|
| **0** | Always allowed (docs, comments, audit) | None |
| **1** | Allowed with logging (skill/command load check) | None, logged |
| **2** | Require failing test OR exemption (behavior trigger) | Test or exemption |
| **3** | Strict TDD (core logic, new features) | Mandatory test first |
