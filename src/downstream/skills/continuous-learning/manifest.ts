import { continuousLearningSkill } from "../../../features/builtin-skills/skills/meta"
import type { SkillManifest } from "../../types"

export const manifest: SkillManifest = {
  name: "continuous-learning",
  skill: continuousLearningSkill,
}
